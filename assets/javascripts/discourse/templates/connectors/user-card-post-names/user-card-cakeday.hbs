{{#if siteSettings.private_cakeday_birthday_enabled}}
  {{#if isBirthday}}
    {{#if isSecret}}
      <EmojiImages
        @list={{siteSettings.private_cakeday_secret_emoji}}
        @title={{secretTitle}}
      />
    {{/if}}
    <EmojiImages
      @list={{siteSettings.private_cakeday_birthday_emoji}}
      @title={{birthdayTitle}}
    />
  {{/if}}
{{/if}}
{{#if siteSettings.private_cakeday_enabled}}
  {{#if isCakeday}}
    <EmojiImages @list={{siteSettings.private_cakeday_emoji}} @title={{cakedayTitle}} />
  {{/if}}
{{/if}}
{{user-age-title userage=userAgeTitle title=userBirthdateTitle}}
